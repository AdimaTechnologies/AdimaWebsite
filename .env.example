# Careers form: submissions (including resume) are sent to hr@adimatechnologies.com
# via the Netlify function /.netlify/functions/send-careers-email.
# Set these in Netlify: Site settings → Environment variables.

# --- Option A: Gmail (recommended for testing) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# Create App Password: Google Account → Security → 2-Step Verification → App passwords.

# --- Option B: GoDaddy Webmail ---
# SMTP_HOST=smtpout.secureserver.net
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=ashokpeddinti9598@gmail.com
# SMTP_PASS=your-Ashok@179

# --- Optional ---
# MAIL_TO=hr@adimatechnologies.com
# SMTP_FROM=your-from-address@yourdomain.com
